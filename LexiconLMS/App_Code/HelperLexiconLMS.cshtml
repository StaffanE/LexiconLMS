using System;
namespace LexiconLMS.Helpers
{
    public class  ShortenStringHelper(string name, int maxLength)
    {
    // your helper can contain code and/or markup, e.g.
    // var msg = "Hello " + name;
    // <p>@msg</p>
    // For more information, visit http://go.microsoft.com/fwlink/?LinkID=204658
    public static string ShortDescription(string target, string text)
    {
        if (name.Length > maxLength)
        {
            name = name.Substring(0, maxLength);
        }
    return String.Format("<p>@name</p>");
    }
}